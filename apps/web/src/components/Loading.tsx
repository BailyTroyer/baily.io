import { FC } from "react";

import Layout from "./Layout";

const Loading: FC = () => {
  return (
    <Layout>
      <main>
        <p>loading...</p>
      </main>
    </Layout>
  );
};

export default Loading;
